[project]
name = "scirssagent"
version = "0.1.3"
description = "FeedMeDaily: local RSS literature triage with profile-driven LLM classification and static reports."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.116.0",
  "httpx>=0.28.1",
  "openai>=1.99.0",
  "pydantic>=2.11.0",
  "python-dotenv>=1.0.1",
  "typer>=0.16.0",
  "uvicorn>=0.35.0",
]

[dependency-groups]
dev = [
  "pillow>=12.2.0",
  "pytest>=8.3.0",
  "ruff>=0.12.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
package = false

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_go_shell.py", "historical_*.py"]
addopts = "-p no:cacheprovider"
