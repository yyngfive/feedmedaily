// DeepSeek's current Flash call name. The provider retired `deepseek-v4-flash`,
// which still resolves to this catalog entry, so UI rules that special-case the
// DeepSeek classifier must use the current id.
export const DEEPSEEK_CLASSIFIER_MODEL_ID = "deepseek-flash";
